#include "../include/so_long.h"

void get_args(int c, char **v, struct s_so_long *game) {
//         if(c > 2) handle_error(TOO_MUCH_ARGUMENTS);
//         if(c < 2) handle_error(TOO_FEW_ARGUMENTS);
//         if(!ft_strsufix(v[1], ".ber")) handle_error(BAD_MAP_EXTENTION);
        (void)c;
        (void)v;
        (void)game;
}
